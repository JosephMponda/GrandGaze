# Django Settings
DEBUG=True
DJANGO_SECRET_KEY=eEf57wHQjmLoxvlB4YWHNVyxNwijmdTOBtfv4ozVS0o
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (PostgreSQL 16)
# Point to your local PostgreSQL or Neon (if using cloud fallback)
DB_NAME=must_emr
DB_USER=must_emr
DB_PASSWORD=must_emr
DB_HOST=localhost
DB_PORT=5432

# Field-Level Encryption Key
# Used to encrypt sensitive fields like National ID, phone numbers (per AGENTS.md §7)
# Can be the same as DJANGO_SECRET_KEY, or a separate Fernet key
# In production, this MUST be a strong, randomly-generated key
CRYPTOGRAPHY_KEY=hmyAFASvCut9y2tU7vUeiDUBTDEWSwkRwRBeo2JmK3I=

# Redis (for cache, sessions, Celery when added in Week 2)
REDIS_URL=redis://localhost:6379/0

# Optional: For deployed environments (Render, Neon, Upstash)
# DATABASE_URL=postgresql://user:pass@neon.tech/must_emr
# REDIS_URL=redis://user:pass@upstash.redis.io:6379/0
